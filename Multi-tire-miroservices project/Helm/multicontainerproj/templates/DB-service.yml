apiVersion: v1
kind: Service
metadata:
  name: db
  labels:
    app: db
spec:
  selector:
    app: db
  type: ClusterIP
  ports:
  - name: "db-service"
    port: 8000
    targetPort: 5432
    protocol: TCP
